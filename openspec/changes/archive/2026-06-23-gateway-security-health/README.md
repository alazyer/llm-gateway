# gateway-security-health

Add incoming request auth validation, enhanced health check with upstream probe, and CORS headers for browser-based tooling. Based on README context: the gateway is primarily used by Claude Code, which sends ANTHROPIC_AUTH_TOKEN/x-api-key headers that are currently ignored.
